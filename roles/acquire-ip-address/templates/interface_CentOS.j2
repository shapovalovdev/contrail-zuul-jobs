BOOTPROTO=dhcp
DEVICE={{ item }}
HWADDR={{ hostvars[inventory_hostname]['ansible_' + item]['macaddress'] }}
ONBOOT=yes
TYPE=Ethernet
USERCTL=no
DEFROUTE={{ 'yes' if default_iface == item else 'no' }}"
PEERDNS=no
